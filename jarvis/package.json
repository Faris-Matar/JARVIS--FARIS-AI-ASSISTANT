{
  "name": "jarvis",
  "version": "1.0.0",
  "description": "JARVIS — Faris's AI assistant. Iron Man HUD, local-first intelligence, persistent memory, full skill suite.",
  "main": "electron/main.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "ui:dev": "cd ui && npm run dev",
    "ui:build": "cd ui && npm install --no-fund --no-audit && npm run build",
    "electron": "electron .",
    "mac": "npm run ui:build && electron ."
  },
  "devDependencies": {
    "electron": "^31.0.0"
  },
  "license": "UNLICENSED",
  "private": true
}
